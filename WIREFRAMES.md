# 低保真线框：四类界面与导航

技术栈：**Vite + React + TypeScript**，单页内通过 `uiPhase` 切换：`intro` → `question` → `review` → `success`。

## 1. 介绍页（Intro）

```
┌─────────────────────────────┐
│  退保免费评估（标题）         │
│  开场说明文案                 │
│  ┌─────────────────────────┐ │
│  │ 短免责声明               │ │
│  └─────────────────────────┘ │
│  隐私提示（小号字）           │
│  [      开始评估      ]      │
└─────────────────────────────┘
```

**导航**：点击「开始评估」→ `question`，`questionIndex = 0`，草稿 `phase = question`。

---

## 2. 单题页（Question）

```
┌─────────────────────────────┐
│  3 / 19    ████████░░  58%   │  ← 进度条
│  分组标签（换组时显示）        │
│  ┌─────────────────────────┐ │
│  │ ● 题干全文               │ │
│  │ ┌─────────────────────┐ │ │
│  │ │ 多行输入              │ │ │
│  │ └─────────────────────┘ │ │
│  └─────────────────────────┘ │
│  [ 上一步 ]    [ 下一步/去审阅 ] │
└─────────────────────────────┘
```

**导航**：

- 「上一步」：索引减 1；若在首题则回到介绍页，草稿 `phase = intro`。
- 「下一步」：校验必填；末题则进入 `review`。
- 换分组时显示 `section-chip`（分组标题）。

---

## 3. 审阅页（Review）

```
┌─────────────────────────────┐
│  审阅与提交                  │
│  按分组折叠列表              │
│  ┌─────────────────────────┐ │
│  │ 分组 A                   │ │
│  │ [ 题1 → 答案摘要 ]（可点）│ │
│  │ [ 题2 → … ]              │ │
│  └─────────────────────────┘ │
│  ☐ 长免责声明（须勾选）      │
│  [ 返回修改 ] [ 提交评估 ]   │
└─────────────────────────────┘
```

**导航**：

- 点击某条 → `question`，`questionIndex` 跳转到对应题。
- 「返回修改」→ 最后一题。
- 「提交评估」→ 勾选后可用；提交后 `console.log` 载荷并进入 `success`。

---

## 4. 成功页（Success）

```
┌─────────────────────────────┐
│         ( ✓ )               │
│       提交成功               │
│       说明文案               │
│  [      返回首页      ]      │
└─────────────────────────────┘
```

**导航**：「返回首页」→ `intro`，草稿重置。

---

## 5. 草稿恢复（Modal）

有未完成的本地草稿时，覆盖层询问「继续填写 / 重新开始」，对应 [`ResumeDraftDialog`](src/components/ResumeDraftDialog.tsx)。

---

## 6. 题库与文案

- 动态题目：[`src/data/questions.json`](src/data/questions.json)（`id` / `group` / `promptText` / `isRequired`）。
- 定稿说明与免责声明：[`src/constants/copy.ts`](src/constants/copy.ts)。

题目数量：**19**（已合并原文重复的交费题）。
